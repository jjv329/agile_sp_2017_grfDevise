<%= form_for(@user_vehicle) do |f| %>
  <% if @user_vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_vehicle.errors.count, "error") %> prohibited this user_vehicle from being saved:</h2>

      <ul>
      <% @user_vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :vehicleType, 'Vehicle Type' %><br>
    <%= f.text_field :vehicleType %>
  </div>
  <div class="field">
    <%= f.label :vehicleColor, 'Vehicle Color' %><br>
    <%= f.text_field :vehicleColor %>
  </div>
  <div class="field">
    <%= f.label :maxRider, 'Max. Riders' %><br>
    <%= f.number_field :maxRider %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
