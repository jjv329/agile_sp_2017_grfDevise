<%= form_for(@user_vehicle) do |f| %>
  <% if @user_vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_vehicle.errors.count, "error") %> prohibited this user_vehicle from being saved:</h2>

      <ul>
      <% @user_vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_area :username %>
  </div>
  <div class="field">
    <%= f.label :vehicleType %><br>
    <%= f.text_area :vehicleType %>
  </div>
  <div class="field">
    <%= f.label :vehicleColor %><br>
    <%= f.text_area :vehicleColor %>
  </div>
  <div class="field">
    <%= f.label :maxRider %><br>
    <%= f.number_field :maxRider %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
