<%= form_for(@ride_request) do |f| %>
  <% if @ride_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride_request.errors.count, "error") %> prohibited this ride_request from being saved:</h2>

      <ul>
      <% @ride_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id, 'UserID' %><br>
    <%= f.text_area :user_id %>
  </div>
  <div class="field">
    <%= f.label :event_list_id, 'EventId' %><br>
    <%= f.text_area :event_list_id %>
  </div>
  <div class="field">
    <%= f.label :confirmed, 'Confirmation Status' %><br>
    <%= f.text_area  :confirmed %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
